import AdminLayout from "@/Layout/AdminLayout";

function reviewId() {
    return (
        <AdminLayout title={`Manage ${'review'}`}>
            Enter
        </AdminLayout>
    );
}

export default reviewId;