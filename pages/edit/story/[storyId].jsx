import AdminLayout from "@/Layout/AdminLayout";

function storyId() {
    return (
        <AdminLayout title={`Manage ${'Story'}`}>
            Enter
        </AdminLayout>
    );
}

export default storyId;